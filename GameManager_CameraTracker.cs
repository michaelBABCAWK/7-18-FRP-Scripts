using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager_CameraTracker : MonoBehaviour
{
    // Start is called before the first frame update

     public bool boosterOverall_ = true;
     public bool cannonOverall_ = true;
     public bool shieldOverall_ = true;

    private void Update()
    {
        
    }
}
